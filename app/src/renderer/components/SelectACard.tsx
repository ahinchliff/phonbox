import * as React from 'react';

const SelectACard: React.FC = () => {
  return (
    <div className="flex flex-1 justify-center items-center">
      <span className="text-lg">Select a card</span>
    </div>
  );
};

export default SelectACard;
